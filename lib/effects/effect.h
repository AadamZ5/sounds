
//Notice how we do not define our Cuda function inside the header file. It is not to be called from user libraries or programs.

namespace DynamicSounds{
    namespace Wrapper{
        void StandardFunction();
    }
}
