/*
SoundSpace is a container class that will hold sounds and their positions, effectively creating a 3D space for sounds to reside in.

SoundSpace is used to hold locations of sounds, and compute amplitudes based on a given location or Listener class.
*/

namespace DynamicSounds{
    class SoundSpace
    {
    private:
        /* ... */
    public:
        SoundSpace();
        ~SoundSpace();

        
    };

}